{% extends 'base.html.twig' %}

{% block title %}Hello LandingController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ random_greetings }}</h1>
    <p>Let's test a simple <strong><em>Symfony microservices with Docker</em></strong> solution ✅</p>

    <br/>

    <h4>What does this page do ?</h4>
    <ul>
        <li>2 Symfony frontend Docker containers (Nginx + PHP) render this simple HTML page</li>
        <li>2 other Symfony API containers (Nginx + PHP) are contacted via HttpClient GET request</li>
        <li>the API returns a simple set of movie characters</li>
        <li>another Docker container (MySQL 8) is used to store the simple data (movie characters)</li>
        <li>the set of characters is randomly shuffled and displayed on this page</li>
    </ul>

    <p style="color: darkblue; font-style: italic; font-weight: bolder;">
        List of loaded movie characters:
    </p>
    {% if characters is not empty %}
        <ul>
            {% for character in characters %}
                <li style="color: mediumblue;">{{ character.name }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <p style="color: red;">Sorry, no characters were loaded (?) Try again ?</p>
    {% endif %}
</div>
{% endblock %}
